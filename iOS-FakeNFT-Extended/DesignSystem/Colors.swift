import UIKit

extension UIColor {
    static let segmentActive = UIColor(resource: .fnText)
    static let segmentInactive = UIColor(resource: .fnLightGray)
    static let closeButton = UIColor(resource: .fnText)
}
